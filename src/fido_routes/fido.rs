use std::convert::Infallible;
// use warp::{Rejection, Reply};
// use warp::http::StatusCode;
// use u2f::{}

pub async fn register_request() {
  // let challenge =
  // let response = warp::reply::with_status("Af Hverju", StatusCode::OK);
  // Ok(response)
}
